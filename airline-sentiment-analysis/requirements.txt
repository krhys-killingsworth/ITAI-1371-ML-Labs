# Core dependencies for the airline sentiment analysis pipeline.
# Pinned to major versions for reproducibility without over-constraining.

pandas>=1.5,<3.0
numpy>=1.23,<3.0
scikit-learn>=1.2,<2.0
matplotlib>=3.6,<4.0
seaborn>=0.12,<1.0

# Notebook execution (not required for src/sentiment_pipeline.py)
jupyter>=1.0
notebook>=6.5
